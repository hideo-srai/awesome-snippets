// @flow

const translations: * = {
  en: {
    apply: 'Apply',
    cancel: 'Cancel',
  },
  es: {
    apply: 'Apply',
    cancel: 'Cancel',
  },
}

export type Translations = typeof translations
export default translations
